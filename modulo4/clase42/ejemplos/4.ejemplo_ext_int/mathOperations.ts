// Aca usamos tipos explicitos en los parametros y el retorno de las funciones
export function add(a:number, b:number): number {
    return a+ b;
}

export function multiply(a: number, b: number): number {
    return a * b;
}